<p id="notice"><%= notice %></p>
<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <%= render(template: 'accounts/_summary.html.erb', locals: { account: @account, current_user: current_user } ) %>
    </div>
  </div>
</div>
<%= link_to 'Edit', edit_account_path(@account) %> |
<%= link_to 'Back', accounts_path %>
