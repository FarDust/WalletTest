<% url = movement.id ? account_movement_path(@account, movement) : account_movements_path(@account) %>
<%= form_with(model: movement, url: url, local: true) do |form| %>
  <% if movement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movement.errors.count, 'error') %> prohibited this movement from being saved:</h2>

      <ul>
        <% movement.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label(:category_id) %>
    <%= form.text_field(:category_id) %>
  </div>

  <div class="field">
    <%= form.label(:account_id) %>
    <%= form.text_field(:account_id) %>
  </div>

  <div class="field">
    <%= form.label(:fecha) %>
    <%= form.datetime_select(:fecha) %>
  </div>

  <div class="field">
    <%= form.label(:final_balance) %>
    <%= form.number_field(:final_balance) %>
  </div>

  <div class="field">
    <%= form.label(:amount) %>
    <%= form.number_field(:amount) %>
  </div>

  <div class="field">
    <%= form.label(:comment) %>
    <%= form.text_area(:comment) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
